Discrimination Task setup

    Both
        n           Number of sets / repetitions
        tDelay      Delay in milliseconds before a prompt, randomly chosen from { 3000, 3500, 4000, 4500, 5000 }

    Cardinal
        randomPool  Pool of prompt directions, randomly chosen from { UP, RIGHT, DOWN, LEFT } 

    Octant
        randomPool  Pool of prompt directions, randomly chosen from { UP, RIGHT, DOWN, LEFT, UP+RIGHT, DOWN+RIGHT, DOWN+LEFT, UP+LEFT } 

    Questions:
        Prompt timing: 
            Should the prompt be sustained until a response is given, or should the prompt only last for X seconds?
